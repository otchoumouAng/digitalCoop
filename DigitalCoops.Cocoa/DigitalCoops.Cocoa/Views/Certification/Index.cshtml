<head>
    <link rel="stylesheet" href="~/Content/IconsStyles.css" />
    <link rel="stylesheet" href="~/Content/CustomStyles.css" />
    <link rel="stylesheet" href="~/Content/bootstrap.css" />
</head>
<script type="text/javascript" src="~/Scripts/renderers.js"></script>
<script type="text/javascript" src="~/Scripts/CommonFunctions.js"></script>
<script type="text/javascript">
    var GetNbrStoreRows = function () {
        var nbr = App.grpListeCertification.getRowsValues().length;
        App.LblResumeCertification.setText(nbr + ' Certification(s)');        
    };

    var SetExcelList = function () {
        var mList = getRowsForExcel(App.grpListeCertification.columnManager.getColumns(), App.grpListeCertification.getRowsValues({ visibleOnly: true }));
        var BaseUrl = App.BaseUrl.value;
        if (mList) {
            Ext.net.DirectMethod.request({
                url: BaseUrl + "/Report/ExportToExcel",
                cleanRequest: true,
                isUpload: true,
                params: {
                    ItemList: mList
                }
            });
            return true;
        }
        else
            return false;
    };

    var SetCriteriaTitle = function (status) {

        App.CertificationCriteriaPanel.title = "";
        var texttitle = "";

        if (status) texttitle += "Show only active Certification : YES ";
        else texttitle += "Show only active Certification : NO ";

        App.CertificationCriteriaPanel.setTitle(texttitle);
    };

    var renderIcon = function (value) {
        var tpl = '<span class="glyphicon {0}" aria-hidden="true"></span>';


        //var tpl = '<span class="x-btn-icon-el {0}" aria-hidden="true"></span>';

        var icon_url = Overview.getBootStrapIcons(value);

        //console.log(Ext.String.format(tpl, icon_url));

        return Ext.String.format(tpl, icon_url);
    }



    var iconRenderer = function (value, metaData, record) {

        metaData.css = Overview.getIcons(value);


    }


    var Overview =
    {
        getIcons: function (value) {
            if (value === 0) {
                return 'icon-delete';//Ext.net.ResourceMgr.getIcon('Icon.BulletCross');//'glyphicon-remove';//Ext.net.ResourceMgr.getIconUrl('Icon.BulletCross');
            }
            else if (value == 1) {
                return 'action_approve_02';// 'glyphicon-ok';//Ext.net.ResourceMgr.getIconUrl('Icon.Tick');
            }
            else if (value == 2) {
                return 'action_new_02';//'glyphicon-file';//Ext.net.ResourceMgr.getIconUrl('Icon.Add'); // New  glyphicon glyphicon-ok
            }
        },
        getBootStrapIcons: function (value) {
            if (value === 0) {
                return 'glyphicon-remove red'; // glyphicon glyphicon-remove red
            }
            else if (value == 1) {
                return 'glyphicon-ok green';
            }
            else if (value == 2) {
                return 'glyphicon-file blue'; // glyphicon glyphicon-file blue
            }
        },
        resetButtons: function () {

            if (App.rowSelectionCertification.hasSelection()) {

                if (App.CrthiddenPermModifier.value == 'true')
                    App.btnModifyCertification.enable();

                if (App.CrthiddenPermDesactiver.value == 'true')
                    App.mnuCancelCertification.enable();

                var isDisabled = App.rowSelectionCertification.selected.items[0].data.Desactive;

                if (isDisabled === true) {
                    App.mnuCancelCertification.setText('Activer');
                    App.btnModifyCertification.disable();

                    if (App.CrthiddenPermActiver.value == 'true')
                        App.mnuCancelCertification.enable();

                    App.mnuCancelCertification.setIconCls('glyphicon glyphicon-ok green');
                }
                else {
                    if (App.CrthiddenPermModifier.value == 'true')
                        App.btnModifyCertification.enable();

                    App.mnuCancelCertification.setText('Annuler');
                    App.mnuCancelCertification.setIconCls('glyphicon glyphicon-remove red');
                }

            } else {

                App.btnModifyCertification.disable();                
                App.mnuCancelCertification.disable();

            }
        }
    }


</script>


@model Tms.Classes.Shared.Certification
@{
    Layout = null;
    var X = Html.X();
    string BaseUrl = (new Tms.Classes.Shared.Parametres(0).Base_url);
}

@(Html.X().ResourceManager())

<form id="tms_form">

    @(X.Viewport()
                    .Layout(LayoutType.Border)
                    .ID("TmsViewPort")
                    .Items(
                        X.Hidden().ID("BaseUrl").Value(@BaseUrl),
                            X.Hidden().ID("CrthiddenPermCreer").ReadOnly(true),
                            X.Hidden().ID("CrthiddenPermModifier").ReadOnly(true),
                            X.Hidden().ID("CrthiddenPermDesactiver").ReadOnly(true),
                            X.Hidden().ID("CrthiddenPermActiver").ReadOnly(true),
                            X.Hidden().ID("CrthiddenPermExporterExcel").ReadOnly(true),
                            X.Hidden().ID("CrthiddenPermOverview").ReadOnly(true),

                        X.Panel()
                            .ID("pnlNorth")
                            .Region(Region.North)
                            .Layout(LayoutType.Border)
                            .TopBar(new Toolbar()
                            {
                                Items = { X.Button()
                            .ID("btnFilterCertification")
                            .IconCls("action_filter_02")
                            .Border(false)
                            .Cls("ButtonStyle")
                            .Text(DigitalCoops.Cocoa.Properties.Resources.btnFilter)
                            .Disabled(false)
                            .DirectEvents( de =>{
                                de.Click.Url  = Url.Action("OnFilter");
                                de.Click.EventMask.ShowMask = true;
                                } ),

                            X.Button()
                            .ID("btnRefreshCertification")
                            .IconCls("action_refresh_01")
                            .Text(DigitalCoops.Cocoa.Properties.Resources.BtnRefresh)
                            .Border(false)
                            .Cls("ButtonStyle")
                            .Disable(false)
                            .DirectEvents( de =>{
                                de.Click.Url  = Url.Action("onRefresh");

                                de.Click.ExtraParams.Add(new Parameter {  Name ="ItemStatus", Encode=true, Value= "#{TxtStatus}.getValue()",     Mode=ParameterMode.Raw});
                                de.Click.Success = "SetCriteriaTitle(App.TxtStatus.value);";
                                de.Click.EventMask.ShowMask = true;




            } ),

                                 X.Button()
                                .ID("btnNewCertification")
                                .IconCls("action_new_02")
                                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnNew)
                                .Cls("ButtonStyle")
                                .Border(false)
                                .Disable(false)
                                .DirectEvents( de =>{
                de.Click.Url  = Url.Action("OnAdd");
                                    de.Click.Success = "App.FormCertification.setTitle('Certification : Nouveau')";
                de.Click.EventMask.ShowMask = true;
            } ),

                             X.Button()
                            .ID("btnModifyCertification")
                            .IconCls("action_modify_02")
                            .Text(DigitalCoops.Cocoa.Properties.Resources.BtnModify)
                            .Cls("ButtonStyle")
                            .Border(false)
                            .Disabled(true)
                            .DirectEvents( de =>{
                de.Click.Url  = Url.Action("OnEdit");
                                de.Click.Success = "App.FormCertification.setTitle('Certification : Modifier')";
                de.Click.ExtraParams.Add(new Parameter { Name="ItemSelected", Mode=ParameterMode.Raw, Encode=true, Value="#{grpListeCertification}.getSelectionModel().getSelection()[0].data"});
                de.Click.EventMask.ShowMask = true;
            } ),
                             X.Button()
                                .ID("btnUpdateCertifContrat")
                                .IconCls("action_new_02")
                                .Text("Bonus Certification (Sales)")
                                .Cls("ButtonStyle")
                                .Border(false)
                                .Hidden(true)
                                .Disable(false)
                                .DirectEvents( de =>{
                de.Click.Url  = Url.Action("OnEditSaleBonus");
                                    de.Click.Success = "App.FormCertification.setTitle('Certification (Sales) : Update Bonus')";
                de.Click.EventMask.ShowMask = true;
            } ),


                                // X.SplitButton()
                                //.ID("btnPrint")
                                //.IconCls("action_print_02")
                                //.Text(DigitalCoops.Cocoa.Properties.Resources.BtnPrint)
                                //.Disable(false)
                                //.Cls("ButtonStyle")
                                //.Border(false)
                                //.Menu(new Menu()
                                //{
                                //    Items =
                                //    {

                                        

                   
                                //        }

                                //})
                                //,

                                X.ToolbarFill(),

                                 X.Button()
                                .ID("btnActions")
                                .Text("Actions")
                                .Border(false)
                                .Cls("ButtonStyle")
                                .IconCls("menu_04")
                                .Disable(false)
                                 .Menu(new Menu()
                                 {
                                     Items = {
                                             X.MenuItem()
                                                .ID("mnuExportListCertification")
                                                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnPrint)
                                                .Icon(Icon.PageExcel)
                                                .Handler("SetExcelList();"),
                                                X.MenuItem()
                                                .ID("mnuCancelCertification")
                                                .IconCls("glyphicon glyphicon-remove red")
                                                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnCancel)
                                                .Cls("ButtonStyle")
                                                .Border(false)
                                                .Disabled(true)
                                                .DirectEvents(de =>
                                                {
                                                    de.Click.Url = Url.Action("OnActivateDeactivate");
                                                    de.Click.ExtraParams.Add(new Parameter { Name = "ItemSelected", Mode = ParameterMode.Raw, Encode = true, Value = "#{grpListeCertification}.getSelectionModel().getSelection()[0].data" });
                                                    de.Click.EventMask.ShowMask = true;
                                                    de.Click.Confirmation.ConfirmRequest = true;
                                                    de.Click.Confirmation.Title = "Certification";
                                                    de.Click.Confirmation.BeforeConfirm = "return SetMsgBoxConfirm(config,#{grpListeCertification}.getSelectionModel().getSelection()[0].data.Desactive)";
                                                    de.Click.Success = "Overview.resetButtons();";
                                                })

                                                }
                                        })
                                    }
                            })

                                ,
                                X.FormPanel()
                                .ID("CertificationCriteriaPanel")
                                .TitleConfig(new PanelTitle { Cls = "title_font_color", Text = "Certification - overview" })
                                .Region(Region.North)
                                .Floatable(false)
                               .Border(false)
                               .Collapsed(true)
                               .Collapsible(true)
                               //.CollapsedCls("panelcollapse")
                               .BodyPadding(5)
                               .Frame(true)
                               .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Middle, Pack = BoxPack.Start })
                                .Items(

                                    X.Panel()
                                        .Border(false)
                                        //.Frame(true)
                                        //.Margin(10)
                                        .Width(400)
                                        .Height(40)
                                        //.Title("Criteria")
                                        .Layout(LayoutType.Form)
                                        //.BodyPadding(6)
                                        .Items(
                                                  X.Checkbox().ID("TxtStatus").FieldLabel("Show only active Certification").LabelAlign(LabelAlign.Right)
                                                ),
                                        X.Container()
                                    .ID("")
                                    .Height(40)
                                    .Flex(1)
                                    .Layout(LayoutType.VBox)
                                    .LayoutConfig(new VBoxLayoutConfig { Align = VBoxAlign.Center, Pack = BoxPack.Center })
                                    .Items(X.Label().ID("LblResumeCertification").Text("Resume ici").EmptyText(""))
                        //X.Panel()
                        //    .ID("formOverviewCertification")
                        //    .Margin(10)
                        //    .Flex(1)
                        //    .Frame(true)
                        //    .Height(100)
                        //    .TitleConfig(new PanelTitle { Text = "Summary", Cls = "title_font_color" })
                        //    .Border(false)
                        //    .BodyPadding(6)
                        //    .Layout(LayoutType.HBox)
                        //    .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Stretch, Pack = BoxPack.Center })
                        //    .Items(X.Container()
                        //        .ID("")
                        //        .Layout(LayoutType.VBox)
                        //        .LayoutConfig(new VBoxLayoutConfig { Pack = BoxPack.Center })
                        //        .Items(X.Label().ID("LblResumeCertification").Text("Resume ici").EmptyText(""))
                        //        )
                        ),


                        X.GridPanel()
                       .Layout(LayoutType.Fit)
                       .ID("grpListeCertification")
                       .Region(Region.Center)
                       .ForceFit(false)
                       .AllowDeselect(false)
                        .Store(
                            X.StoreFor<Tms.Classes.Shared.Certification>()
                                .ID("storeListeCertification")
                                .PageSize(50)
                                .RemotePaging(false)
                            .Proxy(Html.X().AjaxProxy()
                                        .Url(Url.Action("Select"))
                                        .Reader(Html.X().JsonReader().RootProperty("data"))
                                    )
                                    .Listeners(ls =>
                                    {
                                        ls.Load.Handler = "GetNbrStoreRows();";
                                        ls.Add.Handler = "GetNbrStoreRows();";
                                    })
                            )
                    .ColumnModel
                    (
                        Html.X().Column().Text("").DataIndex("mIcon").Renderer("renderIcon").Hideable(false).Width(40).Filterable(false),
                        Html.X().Column().Text("Designation").DataIndex("Designation").Flex(1),
                        X.Column().Text("Utilisation Creation").DataIndex("UtilisateurCreation").Flex(1).Hidden(true),
                        X.DateColumn().Text("Date Creation").DataIndex("DateCreation").Flex(1).Hidden(true).Renderer("renderDateLong"),
                        X.Column().Text("Utilisation Modification").DataIndex("UtilisateurModification").Flex(1).Hidden(true),
                        X.DateColumn().Text("Date Modification").DataIndex("DateModification").Flex(1).Hidden(true).Renderer("renderDateLong")

                    ).BottomBar(
                        Html.X().PagingToolbar()
                        .HideRefresh(true)
                .Plugins(Html.X().ProgressBarPager())
                )
                .Plugins(
                    X.FilterHeader()
                )
                .SelectionModel(
                    X.RowSelectionModel()
                        .ID("rowSelectionCertification")
                        .Mode(SelectionMode.Single)
                        .AllowDeselect(false)
                ).Listeners(l =>
                {
                    l.Select.Fn = "Overview.resetButtons";
                    l.Deselect.Fn = "Overview.resetButtons";

                }
                )
                .DirectEvents(de =>
                {
                    de.RowDblClick.Url = Url.Action("OnConsult");
                    de.RowDblClick.CleanRequest = true;
                    de.RowDblClick.ExtraParams.Add(new Parameter { Name = "ItemSelected", Mode = ParameterMode.Raw, Encode = true, Value = "#{rowSelectionCertification}.getSelected().items[0].data" });
                    de.RowDblClick.EventMask.ShowMask = true;
                }
                )

    )
    )

</form>