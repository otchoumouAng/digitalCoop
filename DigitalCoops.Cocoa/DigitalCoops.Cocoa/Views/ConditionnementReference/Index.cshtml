<head>
    <link rel="stylesheet" href="~/Content/IconsStyles.css" />
    <link rel="stylesheet" href="~/Content/CustomStyles.css" />
    <link rel="stylesheet" href="~/Content/bootstrap.css" />
</head>
<script type="text/javascript" src="~/Scripts/renderers.js"></script>
<script type="text/javascript" src="~/Scripts/CommonFunctions.js"></script>
<script type="text/javascript">
    var SetExcelList = function () {
        var mList = getRowsForExcel(App.grpListeConditionnementReference.columnManager.getColumns(), App.grpListeConditionnementReference.getRowsValues({ visibleOnly: true }));
        var BaseUrl = App.BaseUrl.value;
        if (mList) {
            Ext.net.DirectMethod.request({
                url: BaseUrl + "/Report/ExportToExcel",
                cleanRequest: true,
                isUpload: true,
                params: {
                    ItemList: mList
                }
            });
            return true;
        }
        else
            return false;
    };

    var GetNbrStoreRows = function () {
        var nbr = App.grpListeConditionnementReference.getRowsValues().length;
        App.LblResumeConditionnementReference.setText(nbr + '  Conditionnement Reference(s)');
    };

    var SetCriteriaTitle = function (status) {

        App.ConditionnementReferenceCriteriaPanel.title = "";
        var texttitle = "";

        if (status) texttitle += "Show only active  Conditionnement Reference : YES ";
        else texttitle += "Show only active  Conditionnement Reference : NO ";

        App.ConditionnementReferenceCriteriaPanel.setTitle(texttitle);
    };

    var renderIcon = function (value) {
        var tpl = '<span class="glyphicon {0}" aria-hidden="true"></span>';


        //var tpl = '<span class="x-btn-icon-el {0}" aria-hidden="true"></span>';

        var icon_url = Overview.getBootStrapIcons(value);

        //console.log(Ext.String.format(tpl, icon_url));

        return Ext.String.format(tpl, icon_url);
    }



    var iconRenderer = function (value, metaData, record) {

        metaData.css = Overview.getIcons(value);


    }


    var Overview =
    {
        getIcons: function (value) {
            if (value === 0) {
                return 'icon-delete';//Ext.net.ResourceMgr.getIcon('Icon.BulletCross');//'glyphicon-remove';//Ext.net.ResourceMgr.getIconUrl('Icon.BulletCross');
            }
            else if (value == 1) {
                return 'action_approve_02';// 'glyphicon-ok';//Ext.net.ResourceMgr.getIconUrl('Icon.Tick');
            }
            else if (value == 2) {
                return 'action_new_02';//'glyphicon-file';//Ext.net.ResourceMgr.getIconUrl('Icon.Add'); // New  glyphicon glyphicon-ok
            }
        },
        getBootStrapIcons: function (value) {
            if (value === 0) {
                return 'glyphicon-remove red'; // glyphicon glyphicon-remove red
            }
            else if (value == 1) {
                return 'glyphicon-ok green';
            }
            else if (value == 2) {
                return 'glyphicon-file blue'; // glyphicon glyphicon-file blue
            }
        },
        resetButtons: function () {

            if (App.rowSelectionConditionnementReference.hasSelection()) {
                if (App.CoRhiddenPermModifier.value == 'true')
                    App.btnModifyConditionnementReference.enable();

                if (App.CoRhiddenPermDesactiver.value == 'true')
                    App.mnuCancelConditionnementReference.enable();

                var isDisabled = App.rowSelectionConditionnementReference.selected.items[0].data.Desactive;

                if (isDisabled === true) {
                    if (App.CoRhiddenPermActiver.value == 'true')
                        App.mnuCancelConditionnementReference.enable();

                    App.mnuCancelConditionnementReference.setText('Activer');
                    App.btnModifyConditionnementReference.disable();
                    App.mnuCancelConditionnementReference.setIconCls('glyphicon glyphicon-ok green');
                }
                else {
                    if (App.CoRhiddenPermModifier.value == 'true')
                        App.btnModifyConditionnementReference.enable();

                    App.mnuCancelConditionnementReference.setText('Desactiver');
                    App.mnuCancelConditionnementReference.setIconCls('glyphicon glyphicon-remove red');
                }

            } else {

                App.btnModifyConditionnementReference.disable();
                App.mnuCancelConditionnementReference.disable();

            }
        }
    }


</script>


@model Tms.Classes.Shared.Sales.ConditionnementReference
@{
    Layout = null;
    var X = Html.X();
    string BaseUrl = (new Tms.Classes.Shared.Parametres(0).Base_url);
}

@(Html.X().ResourceManager())

<form id="tms_form">

    @(X.Viewport()
    .Layout(LayoutType.Border)
    .ID("TmsViewPort")
    .Items(
        X.Hidden().ID("BaseUrl").Value(@BaseUrl),
            X.Hidden().ID("CoRhiddenPermCreer").ReadOnly(true),
            X.Hidden().ID("CoRhiddenPermModifier").ReadOnly(true),
            X.Hidden().ID("CoRhiddenPermDesactiver").ReadOnly(true),
            X.Hidden().ID("CoRhiddenPermActiver").ReadOnly(true),
            X.Hidden().ID("CoRhiddenPermExporterExcel").ReadOnly(true),
            X.Hidden().ID("CoRhiddenPermOverview").ReadOnly(true),

        X.Panel()
            .ID("pnlNorth")
            .Region(Region.North)
            .Layout(LayoutType.Border)
            .TopBar(new Toolbar()
            {
                Items = { X.Button()
            .ID("btnFilterConditionnementReference")
            .IconCls("action_filter_02")
            .Border(false)
            .Cls("ButtonStyle")
            .Text(DigitalCoops.Cocoa.Properties.Resources.btnFilter)
            .Disabled(false)
            .DirectEvents( de =>{
                de.Click.Url  = Url.Action("OnFilter");
                de.Click.EventMask.ShowMask = true;
            } ),
            X.Button()
                .ID("btnRefreshConditionnementReference")
                .IconCls("action_refresh_01")
                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnRefresh)
                .Border(false)
                .Cls("ButtonStyle")
                .Disable(false)
                .DirectEvents( de =>{
                    de.Click.Url  = Url.Action("onRefresh");
                    de.Click.ExtraParams.Add(new Parameter {  Name ="ItemStatus", Value= "#{TxtStatus}.getValue()",     Mode=ParameterMode.Raw});
                    de.Click.Success = "SetCriteriaTitle(App.TxtStatus.value);";
                    de.Click.EventMask.ShowMask = true;
                } ),
            X.Button()
                .ID("btnNewConditionnementReference")
                .IconCls("action_new_02")
                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnNew)
                .Cls("ButtonStyle")
                .Border(false)
                .Disable(false)
                .DirectEvents( de =>{
                    de.Click.Url  = Url.Action("OnAdd");
                    de.Click.Success = "App.FormConditionnementReference.setTitle(' Conditionnement Reference : Nouveau')";
                    de.Click.EventMask.ShowMask = true;
                } ),
            X.Button()
                .ID("btnModifyConditionnementReference")
                .IconCls("action_modify_02")
                .Text(DigitalCoops.Cocoa.Properties.Resources.BtnModify)
                .Cls("ButtonStyle")
                .Border(false)
                .Disabled(true)
                .DirectEvents( de =>{
                    de.Click.Url  = Url.Action("OnEdit");
                    de.Click.Success = "App.FormConditionnementReference.setTitle(' Conditionnement Reference : Modifier')";
                    de.Click.ExtraParams.Add(new Parameter { Name="ItemSelected", Mode=ParameterMode.Raw, Encode=true, Value="#{grpListeConditionnementReference}.getSelectionModel().getSelection()[0].data"});
                    de.Click.EventMask.ShowMask = true;
                } ),
            //X.SplitButton()
            //    .ID("btnPrint")
            //    .IconCls("action_print_02")
            //    .Text(DigitalCoops.Cocoa.Properties.Resources.BtnPrint)
            //    .Disable(false)
            //    .Cls("ButtonStyle")
            //    .Border(false)
            //    .Menu(new Menu()
            //    {
            //        Items =
            //        {

            //        }

            //    }),
                X.ToolbarFill(),
                X.Button()
                    .ID("btnActions")
                    .Text("Actions")
                    .Border(false)
                    .Cls("ButtonStyle")
                    .IconCls("menu_04")
                    .Disable(false)
                    .Menu(new Menu()
                        {
                        Items = {
                            X.MenuItem()
                            .ID("mnuExportListConditionnementReference")
                            .Text(DigitalCoops.Cocoa.Properties.Resources.BtnPrint)
                            .Icon(Icon.PageExcel)
                            .Handler("SetExcelList();"),
                                X.MenuItem()
                                .ID("mnuCancelConditionnementReference")
                                .IconCls("glyphicon glyphicon-remove red")
                                .Text("Desactiver")
                                .Cls("ButtonStyle")
                                .Border(false)
                                .Disabled(true)
                                .DirectEvents(de =>
                                {
                                    de.Click.Url = Url.Action("OnActivateDeactivate");
                                    de.Click.ExtraParams.Add(new Parameter { Name = "ItemSelected", Mode = ParameterMode.Raw, Encode = true, Value = "#{grpListeConditionnementReference}.getSelectionModel().getSelection()[0].data" });
                                    de.Click.EventMask.ShowMask = true;
                                    de.Click.Confirmation.ConfirmRequest = true;
                                    de.Click.Confirmation.Title = " Conditionnement Reference";
                                    de.Click.Confirmation.BeforeConfirm = "return SetMsgBoxConfirm(config,#{grpListeConditionnementReference}.getSelectionModel().getSelection()[0].data.Desactive)";
                                    de.Click.Success = "Overview.resetButtons();";
                                })

                                }
                        })
                    }
            }),
        X.FormPanel()
            .ID("ConditionnementReferenceCriteriaPanel")
            .TitleConfig(new PanelTitle { Cls = "title_font_color", Text = " Conditionnement Reference - overview" })
            .Region(Region.North)
            .Floatable(false)
            .Border(false)
            .Collapsed(true)
            .Collapsible(true)
            //.CollapsedCls("panelcollapse")
            .BodyPadding(5)
            .Frame(true)
            .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Middle, Pack = BoxPack.Start })
            .Items(
                    X.Panel()
                        .Border(false)
                        //.Frame(true)
                        //.Margin(10)
                        .Width(400)
                        .Height(40)
                        //.Title("Criteria")
                        .Layout(LayoutType.Form)
                        //.BodyPadding(6)
                        .Items(
                                    X.Checkbox().ID("TxtStatus").FieldLabel("Show only active  Conditionnement Reference").LabelAlign(LabelAlign.Right)
                                ),
                    X.Container()
                            .ID("")
                            .Height(40)
                            .Flex(1)
                            .Layout(LayoutType.VBox)
                            .LayoutConfig(new VBoxLayoutConfig { Align = VBoxAlign.Center, Pack = BoxPack.Center })
                            .Items(X.Label().ID("LblResumeConditionnementReference").Text("Resume ici").EmptyText(""))
                        //X.Panel()
                        //    .ID("formOverviewConditionnementReference")
                        //    .Margin(10)
                        //    .Flex(1)
                        //    .Frame(true)
                        //    .Height(100)
                        //    .TitleConfig(new PanelTitle { Text = "Summary", Cls = "title_font_color" })
                        //    .Border(false)
                        //    .BodyPadding(6)
                        //    .Layout(LayoutType.HBox)
                        //    .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Stretch, Pack = BoxPack.Center })
                        //    .Items(
                        //        )
                        ),
                        X.GridPanel()
                       .Layout(LayoutType.Fit)
                       .ID("grpListeConditionnementReference")
                       .Region(Region.Center)
                       .ForceFit(false)
                       .AllowDeselect(false)
                        .Store(
                            X.StoreFor<Tms.Classes.Shared.Sales.ConditionnementReference>().ID("storeListeConditionnementReference")
                            .PageSize(50)
                            .RemotePaging(false)
                            .Proxy(Html.X().AjaxProxy()
                                        .Url(Url.Action("Select"))
                                        .Reader(Html.X().JsonReader().RootProperty("data"))
                                    )
                                    .Listeners(ls =>
                                    {
                                        ls.Load.Fn = "GetNbrStoreRows";
                                        ls.Add.Fn = "GetNbrStoreRows";
                                    })

                            )
                            .ColumnModel
                            (
                                X.Column().Text("").DataIndex("mIcon").Renderer("renderIcon").Width(40).Hideable(false).Filterable(false),
                                X.Column().Text("Reference").DataIndex("Reference").Flex(1),
                                X.Column().Text("Conditionnement").DataIndex("ConditionnementProduitDesignation").Flex(1),
                                X.Column().Text("Description").DataIndex("Description").Flex(1),
                                X.Column().Text("Utilisation Creation").DataIndex("UtilisateurCreation").Flex(1).Hidden(true),
                                X.DateColumn().Text("Date Creation").DataIndex("DateCreation").Flex(1).Hidden(true).Renderer("renderDateLong"),
                                X.Column().Text("Utilisation Modification").DataIndex("UtilisateurModification").Flex(1).Hidden(true),
                                X.DateColumn().Text("Date Modification").DataIndex("DateModification").Flex(1).Hidden(true).Renderer("renderDateLong")

                            )
                            .BottomBar(
                                Html.X().PagingToolbar()
                                .HideRefresh(true)
                            .Plugins(Html.X().ProgressBarPager())
                            )
                            .Plugins(
                                X.FilterHeader()
                            )
                            .SelectionModel(
                                X.RowSelectionModel()
                                    .ID("rowSelectionConditionnementReference")
                                    .Mode(SelectionMode.Single)
                                    .AllowDeselect(false)
                            )
                            .Listeners(l =>
                            {
                                l.Select.Fn = "Overview.resetButtons";
                                l.Deselect.Fn = "Overview.resetButtons";

                            }
                            )
                            .DirectEvents(de =>
                            {
                                de.RowDblClick.Url = Url.Action("OnConsult");
                                de.RowDblClick.CleanRequest = true;
                                de.RowDblClick.ExtraParams.Add(new Parameter { Name = "ItemSelected", Mode = ParameterMode.Raw, Encode = true, Value = "#{rowSelectionConditionnementReference}.getSelected().items[0].data" });
                                de.RowDblClick.EventMask.ShowMask = true;
                            }
                            )

    )
    )

</form>