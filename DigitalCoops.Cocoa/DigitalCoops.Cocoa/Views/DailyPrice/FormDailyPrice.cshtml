@model Tms.Classes.Business.PrixJournalierViewModel
@{
    Layout = null;
    var X = Html.X();
    var culture = System.Threading.Thread.CurrentThread.CurrentUICulture;
    var numberFormat = culture.NumberFormat;
    string decimalSeparator = numberFormat.NumberDecimalSeparator;
    string thousandsSeparator = numberFormat.NumberGroupSeparator;

}
<head>
    <link rel="stylesheet" href="~/Content/IconsStyles.css" />
    <link rel="stylesheet" href="~/Content/CustomStyles.css" />
    <style type="text/css">
        .my-grid .x-grid3-scroller {
            overflow: scroll;
        }
    </style>
</head>
<script type="text/javascript" src="~/Scripts/FormatNumber.js"></script>
<script type="text/javascript">


        var renderIcon = function (value) {
            var tpl = '<span class="glyphicon {0}" aria-hidden="true"></span>';

            var icon_url = Overview.getBootStrapIcons(value);

            return Ext.String.format(tpl, icon_url);
        };

        var SetNumberSeparator = function () {
            var number = App.TxtPrice.value;
            number = number.replace(/ /g, '');
            number = number.replace(/\B(?=(\d{3})+(?!\d))/g, " ");
            App.TxtPrice.setValue(number.toString());
        };
        var RemoveNumberSeparator = function () {
            var amount = App.TxtPrice.value;
            amount = amount.replace(/\ /g, '');
            App.TxtPrice.setValue(amount);
        };


        var Overview =
        {
            getIcons: function (value) {
                if (value === 0) {
                    return 'icon-delete';/*Ext.net.ResourceMgr.getIcon('Icon.BulletCross');//'glyphicon-remove';//Ext.net.ResourceMgr.getIconUrl('Icon.BulletCross');*/
                }
                else if (value == 1) {
                    return 'action_approve_02';/* 'glyphicon-ok';//Ext.net.ResourceMgr.getIconUrl('Icon.Tick');*/
                }
                else if (value == 2) {
                    return 'action_new_02';/*'glyphicon-file';//Ext.net.ResourceMgr.getIconUrl('Icon.Add'); // New  glyphicon glyphicon-ok*/
                }
            },
            getBootStrapIcons: function (value) {
                if (value === 0) {
                    return 'glyphicon-file green';
                }
                else if (value == 1) {
                    return 'glyphicon-ok green';
                }
                else if (value == 2) {
                    return 'glyphicon-file blue';
                }
            },

            resetButtons: function () {

                /*if (App.DPAhiddenPermSetSite.value == 'true')
                    App.BtnAddSiteToPrice.enable();*/

                /*if (App.rowSelectionSitePrice.hasSelection() && App.DPAhiddenPermRemoveSite.value == 'true') {
                    App.BtnRemoveSiteToPrice.enable();
                } else {
                    App.BtnRemoveSiteToPrice.disable();
                }*/
            }
        };


</script>
@(
    X.Window()
        .ID("FormDailyPrice")
        .Width(800)
        .Modal(true)
        .Resizable(false)
        //.Height(410)
        .Modal(true)
        .Height(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? 210 : 410)
        .CloseAction(Ext.Net.CloseAction.Destroy)
        .Y(20)
        .Title("Prix Journalier : Detail")
        .Items(
                X.FormPanel()
                    .FieldDefaults(d => d.ReadOnly = Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Consult ? true : false)
                    .Layout(LayoutType.VBox)
                    .LayoutConfig(new VBoxLayoutConfig { Align = VBoxAlign.Stretch })
                    .Items(

                                //X.Hidden().ID("DPAhiddenPermSetSite").Value(ViewData["HavPermissionSetSite"]).ReadOnly(true),
                                //X.Hidden().ID("DPAhiddenPermRemoveSite").Value(ViewData["HavPermissionRemoveSite"]).ReadOnly(true),
                                X.Panel().Layout(LayoutType.HBox)
                                .LayoutConfig(new HBoxLayoutConfig { Align = HBoxAlign.Stretch })
                                .Items(
                                        X.Panel().Layout(LayoutType.Form).Flex(1)
                                        .Items(
                                                    X.Hidden().ID("hiddenExecMode").Value(Model._ExecMode),
                                                    X.Hidden().ID("hiddenRowversion").Value(Model._PrixJournalier.RowVersionKey),
                                                    X.Hidden().ID("hiddenNumeroPrice").Value(Model._PrixJournalier.Numero),
                                                    X.Hidden().ID("TxtDailyPriceID").Value(Model._PrixJournalier.ID),
                                                    //X.Hidden().ID("hiddenSite").Value(Model._PrixJournalier.Site.ID),
                                                    X.Hidden().ID("currentcampagneCP").Value(Model._PrixJournalier.Campagne.Designation),
                                                    X.Hidden().ID("TxtOldDateToExtend").Value(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? Model._PrixJournalier.DateEcheance.ToShortDateString() : string.Empty),

                                                    X.Hidden().ID("txtSiteID").Value(Model._PrixJournalier.Site.ID),
                                                    X.TextField().FieldLabel("Site").ID("txtSiteNom").LabelAlign(LabelAlign.Right).Value(Model._PrixJournalier.Site.Nom).ReadOnly(true),
                                                    X.Hidden().ID("hiddenSiteParDefaut").Value(Model._SiteParDefautNom),
                                                    //X.ComboBox()
                                                    //      .ID("_cmbSite")
                                                    //      .FieldLabel("Site")
                                                    //      .DisplayField("Nom")
                                                    //      .ValueField("ID")
                                                    //      .ReadOnly(true)
                                                    //      .MatchFieldWidth(false)
                                                    //      .ListConfig(new BoundList { MinWidth = 300 })
                                                    //      .LabelAlign(LabelAlign.Right)
                                                    //      .TypeAhead(false)
                                                    //      .QueryMode(DataLoadMode.Local)
                                                    //      .TriggerAction(TriggerAction.All)
                                                    //      .ForceSelection(true)
                                                    //      .Value(Model._PrixJournalier.Site.ID)
                                                    //      .Store(
                                                    //              X.Store()
                                                    //              //.Listeners(l => l.Load.Handler = "App._cmbSite.setValue(App._cmbSite.getStore().getAt(0));")
                                                    //              .AutoLoad(true)
                                                    //              .Model(
                                                    //                      Html.X().Model()
                                                    //                      .IDProperty("ID")
                                                    //                      .Fields(
                                                    //                                  new ModelField("ID", ModelFieldType.Int) { Mapping = "ID" },
                                                    //                                  new ModelField("Nom", ModelFieldType.String) { Mapping = "Designation" }
                                                    //                              )
                                                    //                  )
                                                    //      .Proxy(
                                                    //                  Html.X().AjaxProxy()
                                                    //                  .Url(Url.Action("LoadSiteByAccess", "Site"))
                                                    //      .Reader(
                                                    //                  Html.X().JsonReader().RootProperty("data")
                                                    //                  )
                                                    //      )
                                                    //  ),

                                                    //X.ComboBox()
                                                    //    .ID("CropYearID")
                                                    //    .Value(Model._PrixJournalier.Campagne.Designation)
                                                    //    .ReadOnly(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? false : true)
                                                    //    .FieldLabel("Campagne")
                                                    //    .AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                                    //    .DisplayField("Designation")
                                                    //    .ValueField("Designation")
                                                    //    .LabelAlign(LabelAlign.Right)
                                                    //    .ForceSelection(true)
                                                    //    .TypeAhead(false)
                                                    //    .QueryMode(DataLoadMode.Local)
                                                    //    .TriggerAction(TriggerAction.All)
                                                    //    .Store(
                                                    //            X.Store()
                                                    //                .AutoLoad(true)
                                                    //                .Model(
                                                    //                        Html.X().Model()
                                                    //                            .IDProperty("ID")
                                                    //                                    .Fields(
                                                    //                                        new ModelField("Designation", ModelFieldType.Int) { Mapping = "Designation" },
                                                    //                                        new ModelField("Designation", ModelFieldType.String) { Mapping = "Designation" }
                                                    //                                    )
                                                    //                        )
                                                    //                .Proxy(Html.X().AjaxProxy()
                                                    //                    .Url(Url.Action("LoadCampagne", "Campagne"))
                                                    //                    .Reader(Html.X().JsonReader().RootProperty("data"))
                                                    //                ).Listeners(ls =>
                                                    //                {
                                                    //                    ls.Load.Handler = "#{CropYearID}.setValue(#{currentcampagneCP}.value)";
                                                    //                })
                                                    //),

                                                    X.DateField()
                                                        .ID("TxtEntryDate")
                                                        .FieldLabel("Date")
                                                        .AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                                        .Text(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? Model._PrixJournalier.DatePrix.ToString() : DateTime.Now.ToString()).LabelAlign(LabelAlign.Right)
                                                      .ReadOnly(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? false : true)
                                                      ,

                                                    X.TextField().ID("TxtPrice").FieldLabel("Prix").LabelAlign(LabelAlign.Right).AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                                        .Text(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? Model._PrixJournalier.PrixAsString.TrimStart() : string.Empty)
                                                        //.MinValue(0).AllowDecimals(false)
                                                        .MaskRe("[0123456789]")
                                                        //.EnableKeyEvents(true)
                                                        .Listeners(ls =>
                                                        {
                                                            //ls.Blur.Fn = "SetNumberSeparator";
                                                            ls.Blur.Handler = "#{TxtPrice}.setValue(formatNumber(#{TxtPrice}.getValue(), '" + thousandsSeparator + "'));";
                                                            //ls.Blur.Fn = String.Format("#{TxtPrice}.setValue(formatNumber(#{TxtPrice}.getValue(),{0}))");
                                                            ls.Focus.Handler = "if (App.TxtPrice.value) { #{TxtPrice}.setRawValue(unformatNumber(#{TxtPrice}.getValue(),'" + thousandsSeparator + "'));}";
                                                        })
                                                    .ReadOnly(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? false : true)
                                                    ),
                                        X.Panel().Layout(LayoutType.Form).Flex(1)
                                        .Items(
                                                X.DateField().ID("TxtStartDate").FieldLabel("Date Debut").AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                                .Text(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? Model._PrixJournalier.DateDebut.ToString() : DateTime.Now.ToString()).LabelAlign(LabelAlign.Right)
                                                //.MinDate(DateTime.Now)
                                                .ReadOnly(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? false : true)
                                        ,
                                        X.DateField().ID("TxtDueDate").FieldLabel("Date Fin").AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                            .Text(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? Model._PrixJournalier.DateEcheance.ToString() : DateTime.Now.AddDays(1).ToString()).LabelAlign(LabelAlign.Right)
                                        //.MinDate(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? DateTime.Now.AddDays(1) : Model._PrixJournalier.DateDebut.AddDays(1))
                                        ,
                                        X.TextArea()
                                            .ID("TxtCommentaire")
                                            .FieldLabel("Commentaire")
                                            .Hidden(true)
                                            .LabelAlign(LabelAlign.Right)
                                            .Text(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? Model._PrixJournalier.Commentaire.ToString() : string.Empty)
                                            .ReadOnly(Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Extend ? false : true)

                                              )
                                        ),
                                X.Panel().Layout(LayoutType.Fit)
                                .Hidden(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Update || Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Consult ? true : false)
                                .TopBar(new Toolbar()
                                {
                                }),
                                    X.GridPanel()
                                    .Hidden(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.AddNew ? false : true)
                                    .Layout(LayoutType.Fit)
                                    .ID("grpListeSitePrice")
                                    .ForceFit(true)
                                    //.Cls("my-grid")
                                    .Height(190)
                                    .AllowDeselect(true)
                                    .Store(
                                            X.StoreFor<Tms.Classes.Shared.Site>()
                                            .ID("storeListeSitePrice")
                                            .Proxy(Html.X().AjaxProxy()
                                                        .Url(Url.Action("LoadSite", "Site"))
                                                        .Reader(Html.X().JsonReader().RootProperty("data"))
                                                        ).Listeners(ls => ls.Load.Fn = "setRow")
                                            //.Parameters(ps =>
                                            //{
                                            //    ps.Add(new StoreParameter("ItemPrice", "#{TxtDailyPriceID}.getRawValue()", ParameterMode.Raw));
                                            //    ps.Add(new StoreParameter("ItemExecMode", "#{hiddenExecMode}.getRawValue()", ParameterMode.Raw));
                                            //}
                                            //    )
                                            )
                                        .ColumnModel
                                        (
                                            //Html.X().Column().Text("").DataIndex("mIcon").Width(20).Renderer("renderIcon").Width(40).Hideable(false),
                                            Html.X().Column().Text("Site").DataIndex("Nom").Flex(1).Hideable(false)
                                        )
                                        //.Plugins(
                                        //    X.FilterHeader()
                                        //    )

                                        .SelectionModel(
                                                X.CheckboxSelectionModel()
                                                .ID("rowSelectionSitePrice")
                                                .Mode(SelectionMode.Multi)
                                                .AllowDeselect(true)
                                        //.SelectedRow(new SelectedRow("TELCAR BOKITO"))
                                        //    .SelectedRows(rows => rows.FindIndex<SelectedRow>(predicate < "e" >;))
                                        //.SelectedRows(rows => rows.AddRange(Model.InitiallySelectedRows))
                                        )
                                    .Listeners(l =>
                                    {
                                        if (Model._ExecMode != Tms.Components.Settings.EnumsDefinition.eExecMode.Consult)
                                        {
                                            l.Select.Fn = "Overview.resetButtons";
                                            l.Deselect.Fn = "Overview.resetButtons";
                                        }
                                    }
                                        )

                                )
                )
                             .Buttons(
                                X.Button()
                                    .Text("OK")
                                    .ID("BtnSubmitDailyPrice")
                                    .Hidden(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Consult ? true : false)
                                    .DirectEvents(de =>
                                    {
                                        if (Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Extend)
                                        {
                                            de.Click.Url = Url.Action("SubmitExtendFormMethod");
                                            de.Click.Before = "return validNew(App.FormDailyPrice.id);";
                                            de.Click.FormID = "#(return this.up('window').down('form');)";
                                        }
                                        else
                                        {
                                            //de.Click.Url = Url.Action(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Update ? "UpdateFormMethod" : "SubmitFormMethod");
                                            de.Click.Url = Url.Action("SubmitFormMethod");
                                            de.Click.Before = "return validNew(App.FormDailyPrice.id);";
                                            de.Click.FormID = "#(return this.up('window').down('form');)";
                                            de.Click.ExtraParams.Add(new Parameter { Name = "ItemSelected", Mode = ParameterMode.Raw, Encode = true, Value = "App.grpListeSitePrice.getRowsValues({selectedOnly : true})" });
                                        }


                                    })
                            ,
                        X.Button()
                            .Text(Model._ExecMode == Tms.Components.Settings.EnumsDefinition.eExecMode.Consult ? "Fermer" : "Annuler")
                            .Handler("App.FormDailyPrice.doClose()")
                            .Cls("btncancel")

                            )


)

@Scripts.Render("~/bundles/validation")

<script type="text/javascript">
        var validNew = function (winId) {
        /*alert(winId);*/
        /*var winId = #{FormFournisseur}.id;*/
        var areAllrequiredFieldsFilled = isFormValid(winId);

        /*
       *   Vérification de la saisie des champs obligatoires
       */
        if (!areAllrequiredFieldsFilled) {
            Ext.Msg.show({ title: 'Prix Journalier : Data Validation ', icon: Ext.MessageBox.WARNING, msg: 'Veuillez remplir les champs réquis.', buttons: Ext.Msg.OK });
            return false;
        }

        var startdate = App.TxtStartDate.rawValue;
        var duedate = App.TxtDueDate.rawValue;
        var olddate = App.TxtOldDateToExtend.value;

        var arrStartDate = startdate.split("/");
        var datebegin = new Date(arrStartDate[2], arrStartDate[1], arrStartDate[0]);
        var arrEndDate = duedate.split("/");
        var dateend = new Date(arrEndDate[2], arrEndDate[1], arrEndDate[0]);

        if (olddate) {
            var arrOldDate = olddate.split("/");
            var DateOld = new Date(arrOldDate[2], arrOldDate[1], arrOldDate[0]);
        }


        /*if (dateend < datebegin) {
            Ext.Msg.show({ title: 'Prix Journalier : Data Validation ', icon: Ext.MessageBox.WARNING, msg: 'Date Fin must be higher/equal to start date !', buttons: Ext.Msg.OK });
            return false;
        } */

        /*if (App.hiddenExecMode.value == "Extend" && (dateend <= DateOld)) {
            Ext.Msg.show({ title: 'Prix Journalier : Data Validation ', icon: Ext.MessageBox.WARNING, msg: 'Extend Date must be different to old due date !', buttons: Ext.Msg.OK });
            return false;
        }*/

    };


        /*Ext.onReady(function() {
         #{BtnOk}.on(#{BtnOk}.clickEvent,validNew );
        //})*/
        var setRow = function () {
            App.rowSelectionSitePrice.select(App.storeListeSitePrice.find("Nom", App.hiddenSiteParDefaut.value));
        };
</script>







