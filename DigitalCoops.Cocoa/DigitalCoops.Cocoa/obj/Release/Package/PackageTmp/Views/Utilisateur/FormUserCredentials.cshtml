<head>
    <link rel="stylesheet" href="~/Content/IconsStyles.css" />
    <link rel="stylesheet" href="~/Content/CustomStyles.css" />
    <link rel="stylesheet" href="~/Content/bootstrap.css" />
</head>

@model Tms.Classes.Security.Utilisateur
@{
    Layout = null;
    var X = Html.X();
}

@*@(Html.X().ResourceManager())*@

<form id="tms_form">

    @(
    X.Window()
        .ID("FormUserCredentials")
        .Width(400)
        .Resizable(false)
        .Height(200)
        .Modal(true)
        .CloseAction(Ext.Net.CloseAction.Destroy)
        //.X(400)
        .Y(50)
        .Title("Authentification")
        .Layout(LayoutType.Fit)
        .Items(
            Html.X().FormPanel()
                    .Layout(LayoutType.Form)
                    .Border(false)
                    .Items(
                            X.Hidden().ID("hiddenRowversion").Value(Model.RowVersionKey),

                            X.Hidden().ID("Txtuserid").Value(Model.IDUtilisateur),

                            X.TextField().ID("TxtOldpwd").FieldLabel("Ancien Mot de Passe").InputType(Ext.Net.InputType.Password).AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                ,
                            X.TextField().ID("TxtNewpwd").FieldLabel("Nouveau").InputType(Ext.Net.InputType.Password).AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                                ,
                            X.TextField().ID("TxtConfirmpwd").FieldLabel("Confirmer").InputType(Ext.Net.InputType.Password).AllowBlank(false).IndicatorCls("IndicatorRedStar").IndicatorText("*")
                )

            )
        .Buttons(
                    X.Button()
                        .Text("OK")
                        .ID("BtnSubmitPwd")
                        .DirectEvents(de =>
                        {
                            de.Click.Url = Url.Action("SubmitCredential");
                            de.Click.ExtraParams.Add(new Parameter { Name = "userid", Value = "#{Txtuserid}.value", Encode = false, Mode = ParameterMode.Raw });
                            de.Click.Before = "return validNew(App.FormUserCredentials.id);";
                            de.Click.EventMask.ShowMask = true;
                            de.Click.EventMask.Msg = "Chargement ...";
                            de.Click.EventMask.MinDelay = 1000;
                            de.Click.FormID = "#(return this.up('window').down('form');)";
                        }),
                    X.Button()
                        .Text(DigitalCoops.Cocoa.Properties.Resources.BtnCancel)
                        .Handler("App.FormUserCredentials.doClose()")
                        .Cls("btncancel")
                )

    )
</form>

@Scripts.Render("~/bundles/validation")

<script type="text/javascript">
    var validNew = function (winId) {
        /*alert(winId);*/
        /*var winId = #{FormFournisseur}.id;*/
        var areAllrequiredFieldsFilled = isFormValid(winId);

        /*
       *   Vérification de la saisie des champs obligatoires
       */
        if (!areAllrequiredFieldsFilled) {
            console.log('error fileds');
            Ext.Msg.show({ title: 'User : Credential ', icon: Ext.MessageBox.WARNING, msg: 'Veuillez remplir les champs réquis.', buttons: Ext.Msg.OK });
            return false;


        }
        var oldvalue = App.TxtNewpwd.value;        
        var newvalue = App.TxtConfirmpwd.value;        

        if (oldvalue != newvalue) {
            Ext.Msg.show({ title: 'User : Credential ', icon: Ext.MessageBox.WARNING, msg: 'New PassWord and Confirm PassWord is different', buttons: Ext.Msg.OK });
            return false;
        }


    };


        /*Ext.onReady(function() {
         #{BtnOk}.on(#{BtnOk}.clickEvent,validNew );
        //})*/
</script>
