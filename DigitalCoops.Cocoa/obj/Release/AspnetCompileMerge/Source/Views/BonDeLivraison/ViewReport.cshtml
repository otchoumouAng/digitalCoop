@Html.DevExpress().WebDocumentViewer(settings =>
{
    settings.Name = "DeliveryNote";
    settings.ClientSideEvents.Init = "function(s, e) { s.previewModel.reportPreview.zoom(1);}";
    //settings.ClientSideEvents.Init = "function(s, e) { s.previewModel.reportPreview.zoom(1); DeliveryNote.Print();}";    
}).Bind((XtraReport)ViewData["Report"]).GetHtml()
